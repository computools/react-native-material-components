import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },
  checkbox: {
    alignItems: 'center',
    justifyContent: 'center',

    borderWidth: 1,
    borderRadius: 8,
  },
});

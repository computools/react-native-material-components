import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    marginBottom: 16,
  },
  actions: {
    gap: 8,
    flexDirection: 'row',

    paddingTop: 24,
    marginStart: 'auto',
  },
  actionButton: {
    paddingStart: 12,
    paddingEnd: 12,
  },
});
